#include<stdio.h>
#include<string.h>

int main(void)
{
    int n;
    char str[22];

    scanf("%d", &n);
    fflush(stdin);
    scanf("%s", str);

    int k = strlen(str);

    int z = n % k;

    if(z != 0)
    {
        
    }


    printf("%d\n", factorial);

    return 0;
}