#include<stdio.h>

int main(void)
{
    double a, b, media;

    while(1)
    {
        do
        {
            scanf("%d", &a);
            i
        }while((a < 0) || (a > 10));

        do
        {
            scanf("%d", &b);
        }while((b < 0) || (b > 10));

        media = (a + b) / 2.0;
        printf("%d")
    }

    return 0;
}