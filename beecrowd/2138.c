#include <stdio.h>
#include <string.h>

int main(void)
{
    char number[1002];
    while(scanf("%s", number) != EOF)
    {
        int len = strlen(number);

        for(int i = 0; i < len, i++)
        {
            
        }
    }

    return 0;
}