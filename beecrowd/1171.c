#include<stdio.h>

int main(void)
{
    int n;
    scanf("%d", &n);
    int num[n];

    for(int i = 0; i < n; i++)
    {
        scanf("%d", &num[i]);
    }

    int step = 0, smallest = num[0];

    for(int i = 0; i < n; i++)
    {
        
    }

    return 0;
}