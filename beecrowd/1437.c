#include <stdio.h>
#include <math.h>


int main(void)
{
    int n;
    char str[1001];
    while(1)
    {
        scanf("%d%*c", &n);
        if(n == 0) break;

        scanf("%s%*c", str);
        for(int i = 0; i < n; i++)
        {

        }

    }

    return 0;
}