#include<stdio.h>

int main(void)
{
    int x, y, temp;
    scanf("%d %d", &x, &y);

    if(x > y)
    {
        temp = x;
        x = y;
        y = temp;
    }

    for(int i = x; i <)

    return 0;
}