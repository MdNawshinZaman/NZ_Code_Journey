#include <stdio.h>
#include <math.h>
#include <string.h>


int main(void)
{
    int n, case = 1;
    while(scanf("%d", &n) != EOF)
    {
        int num;
        num = (n * (n - 1)) / 2;
        num++;

        if(num == 1)
        {
            printf("Caso %d: %d numeros\n", case, num);
        }
        else87777777777777777777777777777  cv

    }

    return 0;
}