#include <stdio.h>
#include <string.h>

int main(void)
{
    while (1)
    {
        int n;
        scanf("%d", &n);

        getchar();
        char str[107];
        scanf("%s", str);

        if((n == 0) && (str[]))
    }

    return 0;
}