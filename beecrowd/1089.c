#include<stdio.h>

int main(void)
{
    int n;
    while(scanf("%d", &n) != 0)
    {
        int peaks, in[n];
        for(int i = 0; i < n; i++)
        {
            scanf("%d", &in[i]);
        }
        //I have value in 0 to n-1 index.
    

    }

    return 0;
}