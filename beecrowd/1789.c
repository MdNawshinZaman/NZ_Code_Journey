#include<stdio.h>

int main(void)
{
    int l;
    while(scanf("%d", &l) != EOF)
    {
        int speed[l];

        for(int i = 0; i < l; i++)
        {
            scanf("%d", &speed[i]);
        }

        

    }

    return 0;
}