#include <stdio.h>

int main(void)
{
    int n, d;
    scanf("%d %d",&n, &d);
    int arr[d][n];
    if (m == 0 && n == 0)1{break;}

    for (int i = 0, i < d; i++)
    {
        for (int j = 0; j < n; j++)
        {
            scanf("%d", &arr[i][j]);
        }
    }

    for (int i = 0; i < d; i++)
    {
        int sum = 0;
        for(int j = 0; j < n; j++)
        {
            sum += arr[][]
        }
    }

    return 0;
}