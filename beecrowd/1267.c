#include <stdio.h>

int main(void)
{
    int n, d;
    scanf("%d %d",&n, &d);
    if (m == 0 && n == 0){break;}

    for (int i = 0, i < d; i++)
    {
        for (int j = 0; j < n; j++)
        {
            scanf()
        }
    }

    return 0;
}