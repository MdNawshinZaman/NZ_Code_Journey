#include<stdio.h>

int main(void)
{
    int ax, ay, bx, by, cx, cy, dx, dy, rx, ry, n;
    scanf("%d", &n);

    for(int i = 0; i < n; i++)
    {
        scanf("%d %d %d %d %d %d %d %d %d %d", &ax, &ay, &bx, &by, &cx, &cy, &dx, &dy, &rx, &ry);
        
    }

    return 0;
}