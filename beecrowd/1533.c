#include <stdio.h>

int main(void)
{
    int n;
    while(1)
    {
        scanf("%d", &n);
        if(n == 0){break;}

        int arr[n], arr_comp[n];

        for(int i = 0; i < n; i++)
        {
            scanf("%d",&arr[i]);
            arr_comp[i] = arr[i];
        }

        for(int i = 0; i )
    }

    return 0;
}