#include <stdio.h>

int main(void)
{
    int n;
    char m[11];
    while(scanf("%d %s", &n, m) != EOF)
    {
        int sum = 0;
        for(int i = 0; i <  n; i++)
        {

        }
    }

    return 0;
}