#include <cs50.h>
#include <stdio.h>
#include <strign.h>
int main(void)
{
    string names[]={}

    for (int i = 0; i < 7; i++)
    { 
        if (numbers[i] == 9)
        {
            printf("Number found in position %i.\n", i);
            return 0;
        }
    }
    printf("Not found\n");
    return 1;
}