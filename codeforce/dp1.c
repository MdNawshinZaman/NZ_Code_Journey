#include<stdio.h>

int main(void)
{
    int t;
    scanf("%d", &t);

    int n, m;
    for(int i = 0; i < t; i++)
    {
        scanf("%d %d", &n, &m);

        int num[m];
        for(int j = 0; j < m; j++)
        {
            scanf("%d", &num[j]);
        }

    }

    return 0;
}