#include <stdio.h>
#include <cs50.h>
int main (void)
{
    int scores [3];
    scores[0] = 72; //[0]-- index zero
    scores[1] = 73; //[1]-- index one
    scores[2] = 33;

    printf("Average: %f",(scores[0]+scrores[1]+scrores[2])/3.0);
}