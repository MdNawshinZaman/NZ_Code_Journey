#include <stdio.h>

int main(void)
{
    int n;
    scnaf("%d", &n);

    int freq[107] = {0}, arr[107] = {0};
    for(int i = 0; i < n; i++)
    {
        for(int j = 0; j < 107; j++)
        {
            freq[j] = 0;
            arr[j] = 0
        }

    }

    return 0;
}