#include<stdio.h>
#include<math.h>

int main(void)
{
    int a, b;
    scanf("%d %d", &a, &b);
    printf("floor %d / %d = %.lf\n", a, b, floor(a / (float)b));
    printf("floor %d / %d = %.lf\n", a, b, floor(a / (float)b));
    printf("floor %d / %d = %.lf\n", a, b, floor(a / (float)b));


    return 0;
}