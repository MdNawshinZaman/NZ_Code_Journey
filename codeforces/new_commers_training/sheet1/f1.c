#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main(void)
{
    char a[25], b[25];
    scanf("%s %s", a, b);

    int x = strlen(a);
    int y = strlen(b);

    printf("%d\n", );

    return 0;
}