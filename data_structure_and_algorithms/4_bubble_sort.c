#include<stdio.h>

int main(void)
{
    int arr[] = {10, 5, 2, 8, 7};
    int s = sizeof(arr) / sizeof(int);

    for(int i = 0; i < s - 1; i++)
    {
        for(int j = ; j < )
    }
    return 0;
}
