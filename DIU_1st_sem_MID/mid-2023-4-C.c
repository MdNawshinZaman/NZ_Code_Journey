#include<stdio.h>

int main(void)
{
    int n;
    scanf("%d", n);

    for(int i = 0; i < n; i++)
    {
        int m;
        scanf("%d", &m);

        for(int j = 0; j < m; j++)
        {
            
        }
    }

    return 0;
}