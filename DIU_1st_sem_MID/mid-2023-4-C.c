#include<stdio.h>

int main(void)
{
    int n;
    scanf("%d", n);

    for(int i = 0; i < n; i++)
    {
        int m, num, big_2_digit_prime = 0;
        scanf("%d", &m);

        for(int j = 0; j < m; j++)
        {
            scanf("%d", &num);
            

        }
    }

    return 0;
}